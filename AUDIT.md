# گزارش بازبینی (واقع‌بینانه)

این گزارش برمبنای بررسی واقعیِ همین کدیه که در پروژه هست، نه یه چک‌لیست ژنریک.
کارهایی که ادعا می‌کنم انجام دادم واقعاً روی کد اعمال و کامپایل/تست شدن؛ کارهایی
که نیاز به دسترسی مستقیم به سرور شما (SSH)، گیت‌هاب شما، یا اجرای واقعی روی
اینترنت داره رو صادقانه به‌عنوان «کاری که باید خودتون انجام بدید» علامت زدم —
چون من به سرور شما یا حساب گیت‌هابتون دسترسی ندارم.

## 🔴 بحرانی (پیدا و رفع شد)
- **ناسازگاری کامل `database.py` با هندلرها**: یه ابزار دیگه تقریباً همه‌ی توابع
  دیتابیس رو بازنویسی/تغییرنام داده بود بدون این‌که هندلرها رو هم‌زمان آپدیت کنه.
  نتیجه: AttributeError روی تقریباً هر خدمتی. **رفع شد** با جایگزینی `database.py`
  با نسخه‌ی سازگار و تست‌شده (رجوع کن به `DEPLOY.md`).
- **توکن ربات چند بار توی پیام‌های چت افشا شده بود** (از طریق پیست کردن لاگ ترمینال).
  این مسئولیت روی خود کاربره: توکن باید از @BotFather ری‌جنریت (Revoke) بشه.

## 🟠 مهم (در طول این گفتگو رفع شد)
- فیلتر کلمات ممنوعه substring ساده بود (نصفه‌ی یه کلمه‌ی دیگه رو هم می‌گرفت) →
  به الگوی مرز-کلمه (word-boundary) تبدیل شد؛ کپشن عکس/ویدیو رو هم چک می‌کنه؛
  حروف عربی/فارسی رو یکی می‌بینه.
- چند تا از فیلترهای گروه (قفل گروه، فیلتر لینک، ضدفلود، عضویت اجباری، فیلتر
  فحش خودش) توی یه `group` مشترک با فیلتر یکسان ثبت شده بودن، که طبق رفتار
  استاندارد python-telegram-bot باعث می‌شد فقط اولی واقعاً اجرا بشه و بقیه هیچ‌وقت
  چک نشن. هرکدوم `group` جدا گرفتن.
- همین باگ دقیقاً همین‌جوری باعث می‌شد پیام خوش‌آمدگویی هم هیچ‌وقت اجرا نشه
  (چون `check_join_security` زودتر ثبت شده بود و «برنده»ی همون گروه می‌شد).
- بیش از ۱۰ جای مختلف که پیام‌های حاوی نام‌کاربری/متن آزاد رو با `parse_mode="Markdown"`
  می‌فرستادن، بدون escape کردن `_`/`*` کرش می‌کردن (`Can't parse entities`) — از جمله
  نام فایل بک‌آپ که چون همیشه `_` داره، `/backup` و `/backups` همیشه کرش می‌کردن.
- نصب `python-telegram-bot` بدون extra `[job-queue]` باعث می‌شد `conversation_timeout`
  کار نکنه و پنل ادمین توی حالت گیرکرده بمونه.
- سیستم فروش/اشتراک (پرداخت، کارت‌ها، تراکنش‌ها، کدهای تخفیف) **دست‌نخورده و
  بدون تغییر منطق کسب‌وکار** حفظ شد؛ فقط باگ نمایشی (Markdown) و قابلیت جدید
  (اعتبار زمانی کد تخفیف، حذف محتوای صفحه فروش) بهش اضافه شد.

## 🟡 متوسط (این پاس اضافه شد)
- WAL mode + busy_timeout روی SQLite فعال شد (احتمال خطای «database is locked»
  رو کم می‌کنه، مخصوصاً وقتی چندتا کاربر هم‌زمان با ربات کار می‌کنن).
- اتصال دیتابیس حالا روی خطا rollback خودکار می‌کنه (قبلاً commit ناقص ممکن بود
  بمونه).
- اسکریپت بک‌آپ/بازیابیِ اتمیک (`scripts/backup.sh` و `restore.sh`) با دستور
  رسمی `sqlite3 .backup` (نه `cp` ساده) اضافه شد — یعنی حتی وسط نوشتن هم بک‌آپ
  خراب نمی‌شه.
- فایل `.env.example` و `.gitignore` اضافه شد تا سکرت‌ها هیچ‌وقت به گیت‌هاب/جای
  عمومی نرن.

## ⚪️ کارهایی که پیشنهاد داده شده ولی من واقعاً *انجامش نمی‌دم* (چون امکانش نیست)
پرامپتی که فرستادی خیلی گسترده‌ست (VPS hardening واقعی، Docker، CI/CD، پن‌تست
زنده، مانیتورینگ). این‌ها رو صادقانه می‌گم که نمی‌تونم از اینجا واقعاً *اجرا*
کنم چون به سرور/گیت‌هاب شما دسترسی ندارم:
- SSH کردن به سرور و اجرای دستورات هاردنینگ واقعی
- ساخت مخزن GitHub خصوصی و پوش کردن کد
- اجرای واقعی پن‌تست روی سرویس زنده‌ی تلگرام
- راه‌اندازی CI/CD واقعی (GitHub Actions با سکرت‌های واقعی شما)
- مانیتورینگ زنده (Grafana/Prometheus و امثالش) روی سرور شما

اگه بخوای، می‌تونم **فایل‌های آماده**‌ی این بخش‌ها رو هم بسازم (Dockerfile،
docker-compose.yml، workflow فایل GitHub Actions) تا خودت روی سرور/گیت‌هابت
اجراشون کنی — بگو کدوم‌هارو بخوای اول بسازم.

## چک‌لیست آماده‌ی تجاری‌شدن (وضعیت فعلی)
| مورد | وضعیت |
|---|---|
| توکن در کد هاردکد نشده | ✅ (توی `.env`) |
| رمزنگاری شماره کارت | ✅ (Fernet، با هشدار اگه کلید ثابت نباشه) |
| پین پنل ادمین + rate limit تلاش‌ها | ✅ |
| لاگ کامل خطاها (به‌جای کرش خاموش) | ✅ |
| بک‌آپ اتمیک + اسکریپت بازیابی | ✅ (این پاس اضافه شد) |
| systemd برای ری‌استارت خودکار | ✅ فایل نمونه آماده‌ست، نصبش دست خودته |
| Docker | ❌ ساخته نشده (بگو بخوای بسازم) |
| CI/CD واقعی | ❌ نیاز به گیت‌هاب/سکرت‌های شماست |
| مانیتورینگ زنده | ❌ نیاز به سرور شماست |
| هاردنینگ واقعی SSH/فایروال سرور | ❌ باید روی خودِ سرور اجرا بشه (دستورات رو می‌تونم بدم) |
